[project]
name = "geocruncher"
version = "0.4.0"
description = "Geocruncher: a bridge between Gmlib and VK, plus its Flask/Celery API layer."
requires-python = ">=3.12"
dependencies = [
    "DracoPy",
    "celery",
    "flask",
    "forgeo",
    "forgeo-gmlib",
    "forgeo-rigs",
    "lxml",
    "meshio",
    "numpy",
    "pyvista",
    "redis",
    "scikit-image",
    "scipy",
    "sympy",
]

[project.scripts]
geocruncher = "geocruncher.main:main"
api = "api.api:main"

[dependency-groups]
dev = [
    "pytest",
    "watchdog[watchmedo]",
    "pybind11[global]",
    "ruff",
    "ty",
    "pre-commit",
]
server = [
    "gunicorn",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
exclude = ["third_party"]

[tool.hatch.build.targets.wheel]
packages = ["geocruncher", "api"]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
